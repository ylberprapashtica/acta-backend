# Application Environment
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Database Schema (optional)
# Defaults to 'public' in production, 'acta_foughtsave' in development
SCHEMA_NAME=public

# JWT Configuration (REQUIRED)
# Generate a strong random secret (minimum 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters

# API URL (optional)
# Used for generating absolute URLs (e.g., for invoice logo URLs)
# If not set, will use relative URLs
API_URL=http://localhost:3000

